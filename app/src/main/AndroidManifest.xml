<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.local.listapersonagensnaruto_activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListaPersonagensNaruto_Activity">
        <activity android:name=".Splash" android:theme="@style/Theme.SemBarra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PersonagensAkatsuki" />
        <activity android:name=".NinjasAkatsuki" />
        <activity android:name=".MenuInicial"></activity>
        <activity android:name=".PersonagensKonoha" />
        <activity android:name=".NinjasKonoha" />
    </application>

</manifest>